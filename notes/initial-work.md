# Initial Work Items

- Fetch antiX core ISO and treat it as the immutable template.
- Define the overlay directory layout and metadata format that the policy daemon will consume.
- Sketch runit services (policy daemon, overlay cleanup, doas-watchdog) and where their logs live.
- Describe the qrexec-inspired socket protocol for controlling QEMU instances.
- Document what parts of Qubes we are keeping around for reference in `notes/qubes-reference.md`.
